# Security policy

Do not commit VPN private keys, preshared keys, production environment files, database dumps, or credentials.

Use `.env.example` only as a template. Report security issues privately to the repository owner.
